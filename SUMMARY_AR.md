# ملخص عربي سريع للمشروع

<div dir="rtl" align="right">

## ما موضوع المشروع؟

نحن ندرس قوائم الحظر العامة المستخدمة في الأمن السيبراني. هذه القوائم تنشر عناوين IP أو شبكات CIDR أو أسماء نطاقات تعتبرها مصادر مشبوهة أو ضارة. هدفنا ليس أن نقول إن كل عنصر في القائمة مجرم، بل أن نقيس كيف تتغير القوائم، ونحافظ على الدليل، ونقارن بينها، ونفهم طريقة الإزالة أو التصحيح.

سؤال المشروع بصورة بسيطة:

**كيف تتغير قوائم الحظر العامة، وما الذي يمكن أن نعرفه عن العناصر المرتبطة بالدنمارك خلال فترة جمع منظمة؟**

## ماذا يفعل البرنامج؟

البرنامج عبارة عن خط معالجة بيانات بسيط:

1. يحمل نسخة يومية من مصادر عامة ويحفظ الملف الأصلي.
2. يسجل وقت التحميل، والرابط، وحالة HTTP، والحجم، وبصمة SHA-256.
3. يحول الصيغ المختلفة إلى ملف CSV واحد اسمه `observations.csv`.
4. يحسب عدد العناصر، والإضافات، والإزالات، ومدة الظهور، والتداخل بين المصادر.
5. ينتج ملفات CSV ورسومات PNG يمكن استخدامها في التقرير.
6. ينشئ سجلاً مستقلاً لكل تشغيل، ويحتفظ بالوقت، والأوامر، والحالة، والبصمات، ونسخة من التحليل.
7. يقارن آخر تاريخين مختلفين في مجلد مستقل اسمه `comparisons/`.
8. يقرأ أدلة الدنمارك التي يدخلها العضو 4 ويضع تصنيفاً واضحاً مع درجة عدم اليقين.

البرنامج **لا** يفحص جهاز Mac، ولا يفحص العناوين الموجودة في القوائم، ولا يرسل بلاغات، ولا يثبت وحده أن عنواناً ما ضار.

## من أين نجمع البيانات؟

في التشغيل الحالي نستخدم روابط HTTPS عامة، وليس API موثقاً بمفتاح سري:

- FireHOL Level 2: قائمة عامة لعناوين IP و CIDR.
- Spamhaus DROP: نسخة عامة من قائمة DROP لعناوين IPv4.
- blocklist.de: ملف نصي عام لعناوين مصادر الهجمات المبلغ عنها.
- URLhaus: غير مفعل حالياً حتى نراجع طريقة الوصول الحالية وشروط الاستخدام.

مصطلح مهم:

**Public feed** يعني ملفاً عاماً ينشره الموقع ويمكن تحميله. هذا يختلف عن خدمة API تحتاج مفتاحاً أو حساباً.

الحزم الموجودة في `requirements.txt` تسمح بإضافات مستقبلية مثل DNS أو RDAP، ولكن البرنامج الحالي لا يتصل تلقائياً بـ AbuseIPDB أو GreyNoise أو VirusTotal أو RIPEstat.

## لماذا نجمع البيانات 30 يوماً؟

نسخة واحدة تعطينا لقطة فقط. لا نستطيع من لقطة واحدة أن نعرف هل العنصر بقي في القائمة أو أضيف حديثاً أو أزيل ثم عاد.

نحتاج **30 تاريخاً مختلفاً على الأقل** حتى نستطيع:

- مقارنة كل يوم باليوم السابق.
- حساب الإضافات والإزالات والتغيرات.
- حساب نسبة بقاء العنصر في القائمة.
- ملاحظة الاستقرار أو التدوير بين المصادر.
- تقليل خطر بناء نتيجة نهائية على يوم غير عادي.
- توضيح الأيام التي فشل فيها التحميل بدلاً من اعتبارها إزالات.

الفترة الطويلة لا تجعل النتائج صحيحة تلقائياً، لكنها تجعل السؤال الزمني قابلاً للقياس. يجب أن نسجل الأيام الناجحة والفاشلة بوضوح.

## أين تُحفظ البيانات بعد كل تشغيل؟

الأمر اليومي الموصى به هو:

```bash
python scripts/run_daily_pipeline.py
```

ينشئ البرنامج مجلداً جديداً مثل:

`run_records/20260916T120000Z_run_a1b2c3d4/`

ويحتوي على:

- `run_manifest.json`: وقت البداية والنهاية بتوقيت UTC، الأوامر، حالات النجاح أو الفشل، عدد الصفوف، وبصمات SHA-256.
- `run_events.csv`: سجل زمني يوضح ماذا بدأ ومتى انتهى.
- `collection.csv`: الروابط، حالة HTTP، الحجم، البصمة، ومسار كل ملف تم جمعه في هذا التشغيل.
- ملفات `stdout` و`stderr`: المخرجات الفعلية لكل أمر حتى نستطيع فهم الخطأ.
- `normalization_errors.csv`: نسخة من ملاحظات المحلل لهذا التشغيل.
- `analysis/`: نسخة من نتائج التحليل التي كانت موجودة وقت التشغيل.
- `validation_report.json`: نتيجة قائمة التحقق PASS/WARN/FAIL بعد انتهاء التشغيل.
- `run_summary.md`: ملخص سريع لإرساله إلى المجموعة.

الملفات الأصلية تبقى في `data/raw/YYYY-MM-DD/` ولا يتم تعديلها. النتائج التراكمية تبقى في `data/normalized/` و`results/`.

## القائمة التفاعلية للمبتدئين

بدلاً من حفظ الأوامر، يمكن تشغيل قائمة واضحة:

```bash
source .venv/bin/activate
export BLOCKLIST_USER_AGENT='AAU-blocklist-study/0.1 contact: GROUP-EMAIL'
python scripts/project_menu.py
```

أهم الاختيارات:

- `1`: تشغيل الجمع والتحويل والتحليل والمقارنة والتحقق بالكامل.
- `2`: إعادة التحليل من الملفات المحفوظة بدون تحميل جديد.
- `3`: عرض نتيجة تاريخ اليوم حسب UTC.
- `4`: عرض سجل الجمع لليوم مع الوقت والرابط والحجم والبصمة.
- `5`: اختيار تاريخ مثل `2026-09-16` وعرض ملفاته وسجلاته.
- `6`: تشغيل قائمة التحقق من الملفات والصيغ والبصمات والنتائج.
- `7`: مقارنة تاريخ اليوم مع الأمس.
- `8`: اختيار تاريخين للمقارنة.
- `9`: فتح سجل تشغيل مؤرشف.
- `10`: إنشاء نسخة احتياطية ZIP بتاريخ ووقت.
- `11`: عرض حالة المشروع وعدد الصفوف والتواريخ.

يعرض البرنامج وقت جهاز Mac المحلي للتوضيح، لكنه يستخدم UTC كوقت رسمي للبحث وأسماء المجلدات. هذا يمنع اختلاف اليوم بين أعضاء المجموعة. البرنامج لا يخترع أرقاماً: البيانات الناقصة تظهر كتحذير أو `NA`.

لا نحتاج Docker أو قاعدة بيانات في هذه المرحلة. ملفات المصدر الأصلية، وملف CSV، والسجلات، والبصمات، ومجلدات التشغيل المؤرشفة أسهل للمراجعة وإعادة الإنتاج. الأمر `python scripts/archive_project.py` ينشئ نسخة احتياطية إضافية.

## كيف تتم مقارنة اليوم الأول باليوم الثاني؟

بعد وجود تاريخين مختلفين، ينشئ البرنامج مجلداً منفصلاً مثل:

`comparisons/<run-id>/2026-09-16_vs_2026-09-17/`

ويضع داخله:

- `comparison_manifest.json`: التاريخان وقاعدة الاختيار وبصمات الملفات.
- `snapshot_index.csv`: ملف الإدخال السابق والحالي مع الوقت والمسار والبصمة.
- `source_summary.csv`: عدد العناصر، المضاف، المحذوف، الثابت، ونسبة التغير لكل مصدر ونوع.
- `indicator_changes.csv`: قائمة العناصر التي أضيفت أو أزيلت.
- ملف مستقل لكل مصدر مثل `firehol_changes.csv`.
- `comparison_notes.txt` و`changes_by_source.png`.

مجلد `comparisons/` مستقل تماماً عن `results/`. لذلك إذا حدث خطأ في المقارنة يمكن فحصها أو إعادة إنشائها دون تغيير التحليل الرئيسي. إذا شغلنا البرنامج مرتين في نفس اليوم، يحتفظ بكل تشغيل، لكن لا يعتبر التشغيل الثاني يوماً جديداً تلقائياً.

إذا لم ينجح جمع مصدر معيّن في أحد التاريخين، يسجل `source_summary.csv` أن اللقطة مفقودة ويضع `NA` في أعداد التغيّر. لا يعتبر البرنامج فشل التحميل دليلاً على أن كل عناصر المصدر حُذفت.

## تقسيم العمل بين الأعضاء الستة

| العضو | المسؤولية الأساسية | الناتج المطلوب |
|---|---|---|
| 1 | قائد المشروع ودمج التقرير | المقدمة، المشكلة، أسئلة البحث، المتابعة، والنسخة النهائية |
| 2 | البحث النظري والأدبيات | Literature Review، سجل البحث، مصادر IEEE، والإطار النظري |
| 3 | جمع البيانات وتطبيعها | تشغيل collector، حفظ النسخ الأصلية، السجلات، `observations.csv`، والتأكد من قابلية التكرار |
| 4 | تحديد العلاقة بالدنمارك | أدلة التخصيص والجغرافيا وASN وDNS، وملفات التصنيف مع درجة عدم اليقين |
| 5 | التحليل والرسومات | الجداول الإحصائية، persistence، additions/removals، overlap، والرسومات |
| 6 | السياسات والأخلاق والخاتمة | سياسات مزودي القوائم، طريقة التصحيح أو الإزالة، المناقشة، القيود، والخاتمة |

كل عضو يراجع عمل عضو آخر. الملكية تعني أن العضو يقود الجزء، وليست عذراً لعدم فهم بقية المشروع.

## من ينتظر من؟

يوجد اعتماد منطقي بين الأجزاء:

1. العضو 3 يجمع الملفات الأصلية ويطبعها في جدول موحد.
2. العضو 4 يستطيع قراءة السياسات مبكراً، لكن التصنيف النهائي يحتاج إلى قائمة العناصر المطَبَّعة.
3. العضو 5 يستطيع إعداد شكل الرسومات مبكراً، لكن الأرقام النهائية تحتاج إلى 30 يوماً وبيانات العضو 4 عند الحاجة.
4. العضو 6 يستطيع كتابة الجزء النظري والسياسات مبكراً، لكن المناقشة والخاتمة النهائية تنتظر النتائج المراجعة.
5. العضوان 1 و2 يتابعان البنية والمراجع طوال الفترة، ثم يدمجان الفصول بعد مراجعة النتائج.

التسلسل المختصر:

`المصادر العامة ← العضو 3: جمع وتطبيع ← العضو 4: أدلة الدنمارك ← العضو 5: تحليل ورسومات ← العضو 6: مناقشة وخاتمة ← العضو 1: دمج نهائي`

## ماذا أنجزنا حتى الآن؟

في التشغيل الحالي:

- تم تحميل FireHOL وSpamhaus وblocklist.de.
- URLhaus بقي غير مفعل.
- تم تطبيع **89,277 ملاحظة**.
- تم تسجيل **0 ملاحظات parser** في `logs/normalization_errors.csv`.
- تم إنتاج **3 صفوف يومية**؛ وهي تمثل مصادر اليوم الحالي وليست 30 يوماً.
- تم إنتاج **44,746 صف persistence**.
- تم إنتاج **2 صف overlap**.

هذه نتيجة baseline جيدة للتأكد من أن البرنامج يعمل، وليست النتيجة النهائية للمشروع. نحتاج تشغيل الجمع يومياً حتى نصل إلى 30 تاريخاً ناجحاً على الأقل.

ملاحظة عن اختلاف الأرقام: التشغيل الذي أرسلته من جهاز Mac أظهر 89,277 ملاحظة مطبعة و44,746 صف persistence. قد تحتوي النسخة المرفقة من بيانات المشروع على نسخة واحدة من كل مصدر، بينما يحتوي مجلد جهازك على نسختين من تشغيلين في اليوم نفسه؛ لذلك قد يظهر رقم مختلف عند إعادة التشغيل. الرقم الذي يجب استخدامه في التقرير هو الرقم الناتج من مجموعة الملفات الأصلية التي يحتفظ بها الفريق، وليس رقماً مكتوباً يدوياً.

## ما الذي يفعله العضو 3 عملياً؟

العضو 3 يشغل الأمر اليومي الواحد:

```bash
source .venv/bin/activate
export BLOCKLIST_USER_AGENT='AAU-blocklist-study/0.1 contact: GROUP-EMAIL'
python scripts/run_daily_pipeline.py
```

بعد ذلك يرسل إلى المجموعة:

- `run_records/<run-id>/run_summary.md`.
- `run_records/<run-id>/run_manifest.json` عند الحاجة للتفاصيل.
- مجلد المقارنة إذا ظهر `comparison_status=created`.
- ملاحظة عن أي مصدر لم ينجح أو أي يوم مفقود.

## ما الذي يجب أن نقوله للمدرس؟

لدينا خطة قابلة للتكرار: كل يوم نحفظ snapshot مع الوقت والبصمة، ثم نستخدم نفس parser ونفس analyzer، ونكتب أي فشل في السجل. عند نهاية الفترة نقارن الجداول والرسومات مع أدبيات الأمن السيبراني وسياسات المزودين. لن نعرض لقطة اليوم الواحد كأنها نتيجة 30 يوماً، وسنميز بين “ظهر في قائمة عامة” وبين “ثبت أنه ضار أو دنماركي”.

## النتيجة النهائية المطلوبة

نريد تقريراً أكاديمياً يحتوي على:

- مقدمة ومشكلة وأسئلة بحث.
- خلفية تقنية وتشريح لقوائم الحظر.
- Literature Review ومراجع IEEE.
- منهجية جمع البيانات وحفظها وتحليلها.
- Findings بجداول ورسومات مبنية على البيانات.
- Discussion عن المعنى والقيود والأخلاق.
- Conclusion تجيب عن أسئلة البحث بدون مبالغة.
- Table of Contents وروابط داخلية، وفهرس، وقائمة أشكال وجداول، وملاحق.

الملفات التي تشرح التفاصيل موجودة في `README.md` و`CODE_STRUCTURE_README.md`، والملفات الأكاديمية موجودة داخل مجلد `report/`.

## قاعدة السلامة

نستخدم الملفات العامة فقط. لا نفحص عناوين الغير، ولا نرسل طلبات إزالة كاذبة، ولا نضع مفاتيح API داخل Git أو التقرير. إذا احتجنا تمريناً عملياً، نستخدم جهازاً أو VM مملوكاً للمجموعة ومعزولاً عن الإنترنت العام.

</div>
